.vercel
.next
node_modules
out
dist
coverage
*.tsbuildinfo
.env
.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.next-local
.next-local-dev
.next-rbac-check
verification
.codex-run
outputs
.codex
.superpowers
.claude
.agents
.impeccable

# Dead file from the legacy rebuild; a Windows file lock blocks deleting the
# working copy this session. Ignored so `git add -A` won't re-track it.
# Delete the physical file with `del src\lib\legacy-page.ts` once unlocked.
src/lib/legacy-page.ts

# Dead component removed from tracking; a Windows file lock blocked deleting
# the working copy this session. Ignored so `git add -A` won't re-track it.
# Delete the physical file with `del src\components\site\Gallery.tsx` once unlocked.
src/components/site/Gallery.tsx

# Audit screenshots/notes should not be committed.
audit/
.env*

# Daftar anggota berisi NIM (nomor mahasiswa) 71 orang. Data pribadi, tidak
# boleh masuk riwayat git — repo ini milik organisasi dan bisa dibuka banyak
# pengurus. File tetap ada di disk untuk keperluan kerja, cuma tidak diikuti.
ASSET/anggota-hmte.md

# Modul yatim sejak /agenda memakai getOrganizationData(); tidak ada yang
# meng-import. Lock file Windows dari watcher `next dev` menghalangi penghapusan
# fisiknya. Diabaikan supaya `git add -A` tidak melacaknya lagi.
# Hapus dengan `del src\data\events.ts` setelah dev server dimatikan.
src/data/events.ts

# Firebase emulator
firestore-debug.log
firebase-debug.log

# Kunci Admin SDK. Kredensial penuh: jangan pernah masuk repo.
service-account.json

.next-stale-data-build-20260815-1035
.next-stale-v7-check
.next-stale-v7-check
.recovery-residual-20260810
.codex-remote-attachments
.debug
.next-stale-vercel-20260814
ASSET