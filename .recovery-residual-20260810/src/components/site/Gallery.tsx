// Dead component: the gallery section was removed from the landing page and
// this file is no longer imported anywhere. A Windows file lock blocked
// deleting the working copy this session (see .gitignore), so it has been
// reduced to an empty module. Delete the physical file with
// `del src\components\site\Gallery.tsx` once the lock clears.
export {}
